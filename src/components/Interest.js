const Interest = ({ interest }) => {
  return (
    <div className="border border-primary rounded-lg p-2 cursor-default">
      <span className="text-md">{interest}</span>
    </div>
  );
};

export default Interest;
